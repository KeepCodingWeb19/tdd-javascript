# 🧪 Guía de Testing E2E con Playwright

> **Nivel:** Intermedio  
> **Requisitos:** Conocimientos básicos de JavaScript, Jest, pruebas backend, y nociones de frontend.

---

## 📦 Instalación

```bash
npm install --save-dev playwright
npx playwright install
```

Esto instalará Chromium, Firefox y WebKit, permitiéndote ejecutar tests en múltiples navegadores.

---

## 🚀 Primer test E2E

```bash
npx playwright codegen http://localhost:3000
```

Esto abrirá una sesión grabadora para ayudarte a generar código automáticamente.

Ejemplo básico de test:

```js
const { test, expect } = require('@playwright/test');

test('debe mostrar el título correcto en la home', async ({ page }) => {
  await page.goto('http://localhost:3000');
  await expect(page).toHaveTitle(/Mi App/);
});
```

---

## 📁 Estructura recomendada

```
e2e/
├── tests/
│   └── home.spec.js
├── fixtures/
│   └── test-data.json
├── playwright.config.js
```

---

## ⚙️ Configuración básica (`playwright.config.js`)

```js
// playwright.config.js
module.exports = {
  testDir: './e2e/tests',
  timeout: 30000,
  use: {
    headless: true,
    baseURL: 'http://localhost:3000',
  },
};
```

---

## 🧰 Buenas prácticas

- Usa `data-testid` en tus componentes para facilitar la selección de elementos.
- Limpia el entorno antes de cada test: resetea DB, estados o mocks.
- Cubre los flujos críticos: login, registro, compra, errores.
- Automatiza en CI con GitHub Actions, GitLab CI o similar.

---

## 🧩 Recursos extra

- [Documentación oficial de Playwright](https://playwright.dev/)
- [Playwright vs Cypress](https://playwright.dev/docs/intro#playwright-vs-cypress)
- [Testing con Playwright y GitHub Actions](https://playwright.dev/docs/ci)
