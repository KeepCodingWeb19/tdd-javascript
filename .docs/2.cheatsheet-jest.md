# 🧪 Cheatsheet de Testing con Jest

## 🧱 Matchers comunes

```js
expect(value).toBe(expected);
expect(value).toEqual(obj);
expect(array).toContain(item);
expect(string).toMatch(/regex/);
expect(fn).toThrow();
```

## 🕵️‍♂️ Mocks y Spies

```js
const mockFn = jest.fn();
jest.mock('./modulo'); // mocking automático

expect(mockFn).toHaveBeenCalled();
expect(mockFn).toHaveBeenCalledWith(arg1, arg2);
```

## 🔁 Setup / Teardown

```js
beforeEach(() => { /* Setup */ });
afterEach(() => { /* Limpieza */ });
beforeAll(() => { /* Conexiones */ });
afterAll(() => { /* Cierre */ });
```

## ✅ Test async

```js
test('async/await', async () => {
  const res = await fetchData();
  expect(res).toBe('ok');
});

test('promesa', () => {
  return fetchData().then(data => {
    expect(data).toBe('ok');
  });
});
```

## 📊 Cobertura

```bash
npm test -- --coverage
```

---

Más en: [https://jestjs.io/docs/expect](https://jestjs.io/docs/expect)
